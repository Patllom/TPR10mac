# server-only: API origin ที่ผู้ดูแลกำหนด ห้ามใช้ NEXT_PUBLIC_ หรือ derive จาก Host ของ request
# deployment จริงชี้ private API ที่รับ transport ผ่าน trusted reverse proxy ตามคู่มือ
TPR10_API_ORIGIN=https://localhost:4443
